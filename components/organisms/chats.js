import { StyleSheet } from 'react-native';

export default function Chats({ data }) {

  return (
    
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  map: {
    flex: 1,
    width: '100%',
  },
});